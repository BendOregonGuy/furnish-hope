{
  "name": "furnish-hope",
  "version": "0.8.0",
  "private": true,
  "description": "Furnish Hope internal ops platform — monorepo wrapper for api/ and web/.",
  "scripts": {
    "install:all": "npm --prefix api install && npm --prefix web install",
    "build": "npm --prefix api install && npm --prefix web install && npm --prefix web run build && npm --prefix api run build",
    "start": "node api/dist/index.js",
    "typecheck": "npm --prefix api run typecheck && npm --prefix web run typecheck",
    "dev:api": "npm --prefix api run dev",
    "dev:web": "npm --prefix web run dev"
  },
  "engines": {
    "node": "22.x"
  }
}
